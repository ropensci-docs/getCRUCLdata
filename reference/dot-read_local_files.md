# Read a single CRU file into a tidy data.table

Read a single CRU file into a tidy data.table

## Usage

``` r
.read_local_files(file, vars)
```

## Arguments

- file:

  CRU .dat.gz file path or a directory containing one such file.

- vars:

  Named logical vector.

## Value

A tidy data.table.
