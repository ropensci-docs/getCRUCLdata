# Check that at least one variable is requested

Check that at least one variable is requested

## Usage

``` r
.check_vars(vars)
```

## Arguments

- vars:

  Named logical vector of CRU variable selections.

## Value

Invisible NULL.
